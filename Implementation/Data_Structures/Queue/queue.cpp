#include <queue>
using namespace std;

int main()
{
    // DICHIARAZIONE
    queue<int> q;

    // AGGIUNTA ELEMENTO IN CODA
    q.push(17);

    // RIMOZIONE ELEMENTO IN TESTA
    q.pop();

    // PRIMO ELEMENTO
    int first = q.front();
}