#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    cin >> a;
    double p1 = (double) 100 / a;
    double p2 = (double) 100 / (100 - a);
    cout << p1 << endl << p2;
    return 0;
}
